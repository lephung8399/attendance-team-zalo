[project]
name = "attendance-team-zalo-backend"
version = "0.1.0"
description = "Attendance & team-balancing backend (FastAPI modular monolith)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
