# Frontend — Điểm danh & Chia đội

Next.js 16 + TypeScript + Tailwind admin UI. See the [repository root README](../README.md) for the full project overview and setup instructions.

```bash
npm install
cp .env.local.example .env.local   # point NEXT_PUBLIC_API_URL at the backend
npm run dev
```
